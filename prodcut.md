## 平台

Nano Banana Studio

## 需求

我開發一個網站，建立nano banana 的生成圖片，網頁佈局會有三欄，網頁風格為企業形象、用色活潑的風格，提供邊框圓角的設計，使用雙語的方式建立中文(英文)

## 技術棧

- 框架：Nuxt 4.2.1 (with Nitro 2.12.9, Vite 7.2.2 and Vue 3.5.24)、Composition API
- UI 庫：Element Plus (整合 tailwind Grid 系統進行佈局)
- 樣式：CSS Variables 管理主題顏色
- 資料庫: sqlite 3

## 布局

1. **左側 (col-md-3)：提示詞樣板區**
    - 上層區塊配置:
        - 提供一般、企劃、採購、業務、生產等分類使用，以簡潔乾淨的橢圓底色表示
        - 提供分類設定，跑出設定視窗可以將樣本歸類
            - 設定視窗:
				1.提供設定新分類
				2.目前所有樣本進行配對，如果新增分類可以引用需要的樣板
        - 設定key 的圖示，點擊後跑出設定畫面，目前提供gemini的API串接
    - 功能：提供分類多套 Template 切換，可以使用"詞庫設定區"所提供的選項來選擇不同的字詞搭配提示詞。
    - 樣式：關鍵參數需使用 ** (粗體) 標示。
    - 邏輯：點選樣板後，內容會填充至中間的主體區。
    - 編輯: 提供樣板可以編輯，套用詞庫設定區的key名稱
2. **中間 (col-md-6)：核心作業區**
    - 上層區塊配置:
        - 連結Gemini: 帶入樣板的語法，直接gemini查詢畫面，將提示詞結果帶入URL開啟新視窗
            - 網址:@gemini [Tab符號] q={提示詞結果}
            - 網址:[https://chatgpt.com/?prompt={提示詞結果}](https://chatgpt.com/?prompt=%7B%E6%8F%90%E7%A4%BA%E8%A9%9E%E7%B5%90%E6%9E%9C%7D)
    - 框體風格：企業輕藍底 (#f0f7ff)，圓角 15px。
    - Prompt 輸入看板：
		- 一個Prompt輸入框為輸入看板，首先引用樣板文字，在樣板文字中，引用右側詞庫，利用元件內嵌 `el-select` 下拉選單，最好還是保留編輯功能，
			下部分 "主要敘述"為敘述重點，請將這兩個部分放在相同一個輸入框一起。
			(1) 下拉選單圓潤可愛風
    - 提示詞預覽: 展示樣板的組合文字(樣板+主要敘述)並提供複製按鈕，並實作複製該提示詞
    - 功能顯示區：展示生成後的 <img>。
    - 按鈕：大型「開始生成」按鈕，需整合樣板選單值與 主要敘述內容。
    - 歷史紀錄：下方橫向顯示最近 5 筆生成的作品縮圖。
	- 版面風格: 淡藍色
3. **右側 (col-md-3)：詞庫設定區**
    - 上層區塊配置:
        - 管理詞庫:提供編輯、新增與刪除字詞的功能
    - 樣式：使用可愛邊框 (Dashed border具設計感的圓角)，字體設為藍色。
    - 資料結構：建立詞庫下拉選單，儲存名稱為{key,name}，內容為:選單。
    - 功能：主要會帶入提示詞樣板內容，其中會引入"詞庫設定"參數，幫我將主體的詞庫設定參數，使用下拉選單可以挑選

## 實作細節需求

1. 請提供完整的 Vue 組件代碼。
2. 實作一個 `generateImage` 函式，模擬將 `templateSelected + freeInput` 組合後的字串 console.log 出來。
3. 使用 LocalStorage 儲存最近 5 筆生成紀錄。
4. 建立API寫入到sqlite的資料庫中